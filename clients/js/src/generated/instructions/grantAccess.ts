/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { JUPITER_DELEGATE_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const GRANT_ACCESS_DISCRIMINATOR = new Uint8Array([
  66, 88, 87, 113, 39, 22, 27, 165,
]);

export function getGrantAccessDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GRANT_ACCESS_DISCRIMINATOR
  );
}

export type GrantAccessInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAccess extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountRent extends
    | string
    | AccountMeta<string> = 'SysvarRent111111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAccess extends string
        ? WritableAccount<TAccountAccess>
        : TAccountAccess,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountRent extends string
        ? ReadonlyAccount<TAccountRent>
        : TAccountRent,
      ...TRemainingAccounts,
    ]
  >;

export type GrantAccessInstructionData = {
  discriminator: ReadonlyUint8Array;
  user: Address;
};

export type GrantAccessInstructionDataArgs = { user: Address };

export function getGrantAccessInstructionDataEncoder(): FixedSizeEncoder<GrantAccessInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['user', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: GRANT_ACCESS_DISCRIMINATOR })
  );
}

export function getGrantAccessInstructionDataDecoder(): FixedSizeDecoder<GrantAccessInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['user', getAddressDecoder()],
  ]);
}

export function getGrantAccessInstructionDataCodec(): FixedSizeCodec<
  GrantAccessInstructionDataArgs,
  GrantAccessInstructionData
> {
  return combineCodec(
    getGrantAccessInstructionDataEncoder(),
    getGrantAccessInstructionDataDecoder()
  );
}

export type GrantAccessAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAccess extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountRent extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  access?: Address<TAccountAccess>;
  systemProgram?: Address<TAccountSystemProgram>;
  rent?: Address<TAccountRent>;
  user: GrantAccessInstructionDataArgs['user'];
};

export async function getGrantAccessInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAccess extends string,
  TAccountSystemProgram extends string,
  TAccountRent extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: GrantAccessAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAccess,
    TAccountSystemProgram,
    TAccountRent
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  GrantAccessInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAccess,
    TAccountSystemProgram,
    TAccountRent
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    access: { value: input.access ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    rent: { value: input.rent ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.access.value) {
    accounts.access.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            106, 117, 112, 105, 116, 101, 114, 45, 100, 101, 108, 101, 103, 97,
            116, 101, 45, 97, 99, 99, 101, 115, 115,
          ])
        ),
        getAddressEncoder().encode(expectSome(args.user)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.rent.value) {
    accounts.rent.value =
      'SysvarRent111111111111111111111111111111111' as Address<'SysvarRent111111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.access),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.rent),
    ],
    data: getGrantAccessInstructionDataEncoder().encode(
      args as GrantAccessInstructionDataArgs
    ),
    programAddress,
  } as GrantAccessInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAccess,
    TAccountSystemProgram,
    TAccountRent
  >);
}

export type GrantAccessInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAccess extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountRent extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  access: Address<TAccountAccess>;
  systemProgram?: Address<TAccountSystemProgram>;
  rent?: Address<TAccountRent>;
  user: GrantAccessInstructionDataArgs['user'];
};

export function getGrantAccessInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAccess extends string,
  TAccountSystemProgram extends string,
  TAccountRent extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: GrantAccessInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAccess,
    TAccountSystemProgram,
    TAccountRent
  >,
  config?: { programAddress?: TProgramAddress }
): GrantAccessInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountAccess,
  TAccountSystemProgram,
  TAccountRent
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    access: { value: input.access ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    rent: { value: input.rent ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.rent.value) {
    accounts.rent.value =
      'SysvarRent111111111111111111111111111111111' as Address<'SysvarRent111111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.access),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.rent),
    ],
    data: getGrantAccessInstructionDataEncoder().encode(
      args as GrantAccessInstructionDataArgs
    ),
    programAddress,
  } as GrantAccessInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAccess,
    TAccountSystemProgram,
    TAccountRent
  >);
}

export type ParsedGrantAccessInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    access: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
    rent: TAccountMetas[4];
  };
  data: GrantAccessInstructionData;
};

export function parseGrantAccessInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGrantAccessInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      access: getNextAccount(),
      systemProgram: getNextAccount(),
      rent: getNextAccount(),
    },
    data: getGrantAccessInstructionDataDecoder().decode(instruction.data),
  };
}
