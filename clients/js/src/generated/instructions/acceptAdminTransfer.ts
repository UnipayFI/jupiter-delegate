/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { JUPITER_DELEGATE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const ACCEPT_ADMIN_TRANSFER_DISCRIMINATOR = new Uint8Array([
  89, 211, 96, 212, 233, 0, 251, 7,
]);

export function getAcceptAdminTransferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_ADMIN_TRANSFER_DISCRIMINATOR
  );
}

export type AcceptAdminTransferInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountNewAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNewAdmin extends string
        ? WritableSignerAccount<TAccountNewAdmin> &
            AccountSignerMeta<TAccountNewAdmin>
        : TAccountNewAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptAdminTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptAdminTransferInstructionDataArgs = {};

export function getAcceptAdminTransferInstructionDataEncoder(): FixedSizeEncoder<AcceptAdminTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_ADMIN_TRANSFER_DISCRIMINATOR,
    })
  );
}

export function getAcceptAdminTransferInstructionDataDecoder(): FixedSizeDecoder<AcceptAdminTransferInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptAdminTransferInstructionDataCodec(): FixedSizeCodec<
  AcceptAdminTransferInstructionDataArgs,
  AcceptAdminTransferInstructionData
> {
  return combineCodec(
    getAcceptAdminTransferInstructionDataEncoder(),
    getAcceptAdminTransferInstructionDataDecoder()
  );
}

export type AcceptAdminTransferAsyncInput<
  TAccountNewAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  newAdmin: TransactionSigner<TAccountNewAdmin>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAcceptAdminTransferInstructionAsync<
  TAccountNewAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: AcceptAdminTransferAsyncInput<
    TAccountNewAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AcceptAdminTransferInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAdmin: { value: input.newAdmin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            106, 117, 112, 105, 116, 101, 114, 45, 100, 101, 108, 101, 103, 97,
            116, 101, 45, 99, 111, 110, 102, 105, 103,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAdmin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptAdminTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptAdminTransferInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type AcceptAdminTransferInput<
  TAccountNewAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  newAdmin: TransactionSigner<TAccountNewAdmin>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAcceptAdminTransferInstruction<
  TAccountNewAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: AcceptAdminTransferInput<
    TAccountNewAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): AcceptAdminTransferInstruction<
  TProgramAddress,
  TAccountNewAdmin,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAdmin: { value: input.newAdmin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAdmin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptAdminTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptAdminTransferInstruction<
    TProgramAddress,
    TAccountNewAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedAcceptAdminTransferInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    newAdmin: TAccountMetas[0];
    config: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: AcceptAdminTransferInstructionData;
};

export function parseAcceptAdminTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAcceptAdminTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      newAdmin: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAcceptAdminTransferInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
