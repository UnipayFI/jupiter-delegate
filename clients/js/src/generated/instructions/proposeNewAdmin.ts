/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { JUPITER_DELEGATE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const PROPOSE_NEW_ADMIN_DISCRIMINATOR = new Uint8Array([
  232, 189, 155, 60, 4, 68, 17, 188,
]);

export function getProposeNewAdminDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_NEW_ADMIN_DISCRIMINATOR
  );
}

export type ProposeNewAdminInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountCurrentAdmin extends string | AccountMeta<string> = string,
  TAccountProposedAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCurrentAdmin extends string
        ? WritableSignerAccount<TAccountCurrentAdmin> &
            AccountSignerMeta<TAccountCurrentAdmin>
        : TAccountCurrentAdmin,
      TAccountProposedAdmin extends string
        ? ReadonlyAccount<TAccountProposedAdmin>
        : TAccountProposedAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeNewAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProposeNewAdminInstructionDataArgs = {};

export function getProposeNewAdminInstructionDataEncoder(): FixedSizeEncoder<ProposeNewAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PROPOSE_NEW_ADMIN_DISCRIMINATOR })
  );
}

export function getProposeNewAdminInstructionDataDecoder(): FixedSizeDecoder<ProposeNewAdminInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProposeNewAdminInstructionDataCodec(): FixedSizeCodec<
  ProposeNewAdminInstructionDataArgs,
  ProposeNewAdminInstructionData
> {
  return combineCodec(
    getProposeNewAdminInstructionDataEncoder(),
    getProposeNewAdminInstructionDataDecoder()
  );
}

export type ProposeNewAdminAsyncInput<
  TAccountCurrentAdmin extends string = string,
  TAccountProposedAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  currentAdmin: TransactionSigner<TAccountCurrentAdmin>;
  proposedAdmin: Address<TAccountProposedAdmin>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getProposeNewAdminInstructionAsync<
  TAccountCurrentAdmin extends string,
  TAccountProposedAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: ProposeNewAdminAsyncInput<
    TAccountCurrentAdmin,
    TAccountProposedAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ProposeNewAdminInstruction<
    TProgramAddress,
    TAccountCurrentAdmin,
    TAccountProposedAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    currentAdmin: { value: input.currentAdmin ?? null, isWritable: true },
    proposedAdmin: { value: input.proposedAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            106, 117, 112, 105, 116, 101, 114, 45, 100, 101, 108, 101, 103, 97,
            116, 101, 45, 99, 111, 110, 102, 105, 103,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.currentAdmin),
      getAccountMeta(accounts.proposedAdmin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeNewAdminInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposeNewAdminInstruction<
    TProgramAddress,
    TAccountCurrentAdmin,
    TAccountProposedAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ProposeNewAdminInput<
  TAccountCurrentAdmin extends string = string,
  TAccountProposedAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  currentAdmin: TransactionSigner<TAccountCurrentAdmin>;
  proposedAdmin: Address<TAccountProposedAdmin>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getProposeNewAdminInstruction<
  TAccountCurrentAdmin extends string,
  TAccountProposedAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
>(
  input: ProposeNewAdminInput<
    TAccountCurrentAdmin,
    TAccountProposedAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ProposeNewAdminInstruction<
  TProgramAddress,
  TAccountCurrentAdmin,
  TAccountProposedAdmin,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? JUPITER_DELEGATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    currentAdmin: { value: input.currentAdmin ?? null, isWritable: true },
    proposedAdmin: { value: input.proposedAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.currentAdmin),
      getAccountMeta(accounts.proposedAdmin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeNewAdminInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposeNewAdminInstruction<
    TProgramAddress,
    TAccountCurrentAdmin,
    TAccountProposedAdmin,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedProposeNewAdminInstruction<
  TProgram extends string = typeof JUPITER_DELEGATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    currentAdmin: TAccountMetas[0];
    proposedAdmin: TAccountMetas[1];
    config: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ProposeNewAdminInstructionData;
};

export function parseProposeNewAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedProposeNewAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      currentAdmin: getNextAccount(),
      proposedAdmin: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeNewAdminInstructionDataDecoder().decode(instruction.data),
  };
}
